controller_credentials:
- credential_type: Ansible Galaxy/Automation Hub API Token
  inputs:
    url: https://galaxy.ansible.com/
  name: Ansible Galaxy
  organization: Default
- credential_type: Source Control
  inputs:
    password: '{{ vaulted_controller_credentials_github_password }}'
    username: KushalSankhe
  name: github
  organization: Default
- credential_type: Red Hat Ansible Automation Platform
  inputs:
    host: https://aap-27-giv-aap-sbx.apps.ocpv.citiuscloud.local
    password: '{{ vaulted_controller_credentials_sandbox_aap_admin_password }}'
    request_timeout: "60"
    username: kushal
    verify_ssl: false
  name: Sandbox AAP Admin
  organization: Default
- credential_type: GitHub Personal Access Token
  inputs:
    token: '{{ vaulted_controller_credentials_github_token_token }}'
  name: github-token
  organization: Default
